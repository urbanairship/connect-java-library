package com.urbanairship.connect.client.model;

public enum EventType {
        OPEN,
        CLOSE,
        CUSTOM,
        LOCATION,
        SEND,
        TAG_CHANGE,
        FIRST_OPEN,
        UNINSTALL,
        PUSH_BODY,
        REGION,
        RICH_DELETE,
        RICH_DELIVERY,
        RICH_READ
}
