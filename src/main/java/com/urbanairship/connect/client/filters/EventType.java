package com.urbanairship.connect.client.filters;

public enum EventType {
        OPEN,
        CLOSE,
        CUSTOM,
        LOCATION,
        SEND,
        TAG_CHANGE,
        FIRST_OPEN,
        UNINSTALL
}
